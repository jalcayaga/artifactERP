{
  "compilerOptions": {
    "target": "es5",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "noEmit": true, // Next.js handles transpilation
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler", // Recommended for Next.js 13+
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve", // Required for Next.js
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"], // Points to frontend/* (e.g. @/components/ui/card -> frontend/components/ui/card.tsx, @/lib/* -> frontend/lib/*, @/contexts/* -> frontend/contexts/*)
      // If you keep custom components in `src/components`, you might need:
      // "@/components/*": ["./src/components/*"], 
      // And shadcn components would be imported from:
      // "@/components/ui/*" (resolves to frontend/components/ui/* if components.json alias is "@/components")
      // Or adjust components.json to use "@/src/components" as its base if that's where shadcn installs.
      // For now, this assumes custom components are also moved to frontend/components or imports are adjusted.
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}