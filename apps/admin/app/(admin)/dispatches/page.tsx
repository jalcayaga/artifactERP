import DispatchView from '@/components/dispatches/DispatchView';

export const metadata = {
    title: 'Guías de Despacho | Artifact ERP',
    description: 'Gestión logística y traslado de mercaderías',
};

export default function DispatchesPage() {
    return <DispatchView />;
}
